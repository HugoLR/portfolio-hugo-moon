---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`flex flex-col gap-6 overflow-hidden border border-text-tertiary rounded-lg shadow-sm hover:shadow-lg transition-all duration-300 ease-out h-full ${className}`}>
  <slot />
</div>
